[tool.black]
line-length = 100

[tool.isort]
profile = "black"

[tool.flake8]
max-line-length = 100
per-file-ignores = {
  "core/agents/base_agent.py" = ["E731", "E501"],
  "core/orchestrator.py" = ["E501"],
}
